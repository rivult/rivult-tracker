# RUNTIME dependencies of the packaged app.
#
# The parser itself is stdlib-only and stays that way. These are what the
# DESKTOP SHELL needs, and they exist in this file because their absence is
# silent: app.py imports webview inside a try/except and degrades to opening a
# browser tab, so a build without them still runs, still serves the dashboard,
# and still passes every test - it just has no native window and no
# close-to-tray. That is exactly how v0.12.0 shipped broken.
#
# Pinned to the versions the 0.11.0 release was built against.
pywebview==6.2.1
pythonnet==3.1.0

# Build/test only
pyinstaller==6.21.0
pytest
